# This project was generated with 0.4.1 using template: https://github.com/beeware/briefcase-template @ v0.4.1
[tool.briefcase]
project_name = "DMB AI Microscope"
bundle = "com.dmb.ai"
version = "0.0.1"
url = "https://github.com/DMB13/AI_Microscope-main.git"
license.file = "LICENSE"
author = "Devis Byaru"
author_email = "devisbyaru13@gmail.com"

[tool.briefcase.app.dmbaimicroscope]
formal_name = "DMB AI Microscope"
description = "AI-Powered Diagnostic Assistant For Clinical Bacterial Classfication"
long_description = """More details about the app should go here.
"""
sources = [
    "src/dmbaimicroscope",
]
test_sources = [
    "tests",
]

requires = [
    # Add your cross-platform app requirements here
]
test_requires = [
    # Add your cross-platform test requirements here
]

[tool.briefcase.app.dmbaimicroscope.macOS]
universal_build = true
requires = [
    # Add your macOS-specific app requirements here
]

[tool.briefcase.app.dmbaimicroscope.linux]
requires = [
    # Add your Linux-specific app requirements here
]

[tool.briefcase.app.dmbaimicroscope.linux.system.debian]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.dmbaimicroscope.linux.system.rhel]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.dmbaimicroscope.linux.system.suse]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.dmbaimicroscope.linux.system.arch]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.dmbaimicroscope.linux.flatpak]
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "25.08"
flatpak_sdk = "org.freedesktop.Sdk"

[tool.briefcase.app.dmbaimicroscope.windows]
requires = [
    # Add your Windows-specific app requirements here
]

# Mobile deployments
[tool.briefcase.app.dmbaimicroscope.iOS]
requires = [
    # Add your iOS-specific app requirements here
]

[tool.briefcase.app.dmbaimicroscope.android]
requires = [
    # Add your Android-specific app requirements here
]

# Web deployments
[tool.briefcase.app.dmbaimicroscope.web]
requires = [
    # Add your web-specific app requirements here
]

